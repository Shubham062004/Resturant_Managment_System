# Frontend Features

This folder hosts domain-specific modules (e.g., `auth/`, `orders/`, `menu/`, `tables/`).
Each feature folder should contain its own components, slices, services, hooks, and types.

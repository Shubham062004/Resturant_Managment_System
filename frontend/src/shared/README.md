# Frontend Shared

This folder hosts global reusable items:

- `components/`: UI components (Button, Input, Card, Modal, Table, etc.)
- `utils/`: Formatting, math, and general helper scripts
- `types/`: Common type interfaces and entities
- `constants/`: Global strings, endpoint URLs, and configuration maps
